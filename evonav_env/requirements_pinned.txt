# Pinned dependency set for CrowdNav++ / Evonav env (Windows, Python 3.10).
# Install into the single project environment: evonav_env/.venv.
# Adjust the cu1xx suffix to match your local CUDA driver.
#
# Upstream README targets Python 3.6/3.8 + PyTorch 1.12.1 + gym 0.15.7.
# Adjustments from strict requirements.txt for py3.10 wheel availability:
#   - numpy 1.20.3 -> 1.23.5 (no py3.10 wheel for 1.20.x)
#   - tensorflow-gpu -> tensorflow==2.11.0 (GPU optional; CPU OK for smoke)
# gst_updated/requirements.txt pins an older stack (torch 1.7.1, TF 2.6);
# use the GPU torch wheel below for the main project and install GST runtime needs.

numpy==1.23.5
pandas==1.5.2
gym==0.15.7
torch==2.11.0+cu128
# Official wheel index used on the verified machine; adjust cu1xx suffix to match your local CUDA driver.
--extra-index-url https://download.pytorch.org/whl/cu128
matplotlib==3.6.2
Cython==0.29.36
scipy==1.10.1
Pillow==9.5.0
PyYAML==6.0.1
tensorboard==2.11.2
protobuf==3.19.6
tensorflow==2.11.0
cloudpickle==1.2.2  # gym 0.15.x compatible
opencv-python==4.8.1.78
tqdm==4.66.1

# EvoNav Algorithm 1 (optional — only for --llm groq)
groq>=1.0.0

# Development / CI
pytest>=7.0.0
